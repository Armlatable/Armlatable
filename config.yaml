
# Locomo Robot Configuration
robot:
  max_velocity: 0.5   # [m/s]
  max_angular_rate: 1.0 # [rad/s]
  timeout_ms: 500

wifi:
  ssid: "LocomoDongle"
  password: "locomorobot"
  udp_port: 8888
  channel: 1
  broadcast_ip: [192, 168, 4, 255]

serial:
  baud_rate: 115200

dongle:
  wifi_channel: 1 # Deprecated/Duplicate, kept for reference if needed, but wifi.channel is preferred
  esp_now_peer_mac: "FF:FF:FF:FF:FF:FF" # Robot MAC Address
